import PagesTitle from "@/components/pages-title";
import StyleProvider from "@/components/style-provider";

export default function Community() {
          return (
                    <StyleProvider>
                              <PagesTitle title='Community' description='Join our community to stay updated on our latest events and connect with other volunteers!' />
                    </StyleProvider>
          )
}