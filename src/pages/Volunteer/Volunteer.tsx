import PagesTitle from "@/components/pages-title";
import StyleProvider from "@/components/style-provider";

export default function Volunteer() {
          return (
                    <StyleProvider>
                              <PagesTitle title='Volunteers' description='Our volunteers are the heart of our organization and we are grateful for their dedication and commitment to our mission.' />
                    </StyleProvider>
          )
}