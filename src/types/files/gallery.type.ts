export type TGallery = {
          _id: string;
          image: {
                    url: string;
                    publicId: string;
          };
          createdAt?: string;
          updatedAt?: string;
};